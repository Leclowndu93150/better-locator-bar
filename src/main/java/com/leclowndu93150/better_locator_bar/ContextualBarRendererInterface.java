package com.leclowndu93150.better_locator_bar;

import com.mojang.blaze3d.platform.Window;

public interface ContextualBarRendererInterface {
    int callLeft(Window window);
    int callTop(Window window);
}